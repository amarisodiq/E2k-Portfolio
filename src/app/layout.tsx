import type { Metadata } from 'next'
import './globals.css'
import Header from '@/components/Header'
import Footer from '@/components/Footer'

export const metadata: Metadata = {
  title: 'E2K Photography - Capturing Your Story',
  description: 'Professional photography services for weddings, portraits, events, and more.',
  keywords: 'photography, wedding photographer, portrait photographer',
  authors: [{ name: 'E2K Photography' }],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}